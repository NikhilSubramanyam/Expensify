console.log("Util.js is running");

const square = x => x * x;

const add = (a, b) => a + b;

const subtract = (a, b) => a - b;

//export default subtract; or export {subtract as default}
//export { square, add }; or export const square =x=> x * x;
